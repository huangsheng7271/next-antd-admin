import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
    token: {
        colorPrimary: '#14b974',
        colorLink: '#14b974',
        borderRadius: 4,
    },
    components: {
        Modal:{
            borderRadiusLG: 6,
        },
        Statistic: {
            contentFontSize: 14,
        },
    }
};

export default theme;

{

}

